<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:my="clr-namespace:AgiliTrain.PhoneyTools.Controls"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                    mc:Ignorable="d">
  <DataTemplate x:Name="SimpleLongListSelectorGroupTemplate">
    <Border Background="{StaticResource PhoneAccentBrush}"
            Padding="4"
            Margin="4">
      <TextBlock Style="{StaticResource PhoneTextGroupHeaderStyle}"
                 Text="{Binding GroupName}" />
    </Border>
  </DataTemplate>
  <DataTemplate x:Name="SimpleLongListSelectorItemTemplate">
    <TextBlock Style="{StaticResource PhoneTextNormalStyle}"
               Text="{Binding}" />
  </DataTemplate>
</ResourceDictionary>
