<UserControl x:Class="PhoneyTools.Example.Views.SimpleLongListSelectorExample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:my="clr-namespace:AgiliTrain.PhoneyTools.Controls;assembly=AgiliTrain.PhoneyTools"
             mc:Ignorable="d"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             d:DesignHeight="480"
             d:DesignWidth="480">
  <Grid x:Name="LayoutRoot"
        Background="{StaticResource PhoneChromeBrush}">
    <my:SimpleLongListSelector x:Name="longList" />
    <!-- Example of using an ItemTemplate -->
    <!--<my:SimpleLongListSelector x:Name="longList">
      <my:SimpleLongListSelector.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <Image Source="{Binding ImageUrl}"
                    Height="75" />
            <TextBlock Style="{StaticResource PhoneTextNormalStyle}"
                        Text="{Binding Name}" />
          </StackPanel>
        </DataTemplate>
      </my:SimpleLongListSelector.ItemTemplate>
    </my:SimpleLongListSelector>-->
  </Grid>
</UserControl>
